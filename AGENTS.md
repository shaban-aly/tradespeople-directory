# قواعد الكود — دليل الصنايعية

قواعد إلزامية لأي جلسة عمل (سواء إنسان أو أداة AI) على هذا المشروع.
اتبعها دائمًا ولا تعدّل عليها إلا بعد مناقشة واضحة.

## قواعد المشروع

1. **افصل المنطق عن التصميم**
   - أي كومبوننت بيعرض UI ميبقاش فيه منطق معقد (fetch, حسابات, شروط كتير).
   - المنطق يتوضع في hooks أو دوال منفصلة (`lib/`)، والكومبوننت يركز بس
     على العرض.

2. **الـ hooks في ملفات منفصلة**
   - كل custom hook في ملفه الخاص داخل مجلد `hooks/`
     (مثال: `hooks/useCraftsmen.ts`)، ولا يُكتب hook داخل الكومبوننت.

3. **فصّل الأجزاء المتكررة أو الكبيرة**
   - أي عنصر UI (كارت صنايعي، زرار اتصال، شارة "موثّق"، أيقونة تخصص) يبقى
     كومبوننت مستقل في ملفه داخل `components/`، مش مكتوب inline داخل صفحة
     كبيرة واحدة.

4. **استخدم تقسيمة المجلدات القياسية في Next.js App Router**
   - `app/` للصفحات، `components/` للكومبوننتس، `hooks/` للـ hooks،
     `lib/` للبيانات والدوال، ولا تُضِف تقسيمات غريبة خاصة بالمشروع.
   - الكومبوننتس تُنظّم في مجلدات: المشترك (المُستخدَم في أكثر من صفحة) داخل
     `components/shared/`، وأقسام كل صفحة في مجلد باسمها
     (`components/home/`, `components/category/`, `components/join/`).
   - الاستيرادات بين الكومبوننتس تكون بالمطلق من `@/components/...`.

5. **شريط التنقل العلوي والـ theme toggle**
   - `Header`, `Footer`, وزر تبديل الفاتح/الغامق (`ThemeToggle`) كلها في
     `components/shared/`.
   - الثيم الفاتح/الغامق مبني على CSS variables في `app/globals.css`
     (كلاس `.dark` على `<html>`)، والتبديل محفوظ في `localStorage`
     عبر `hooks/useTheme.ts`.

## قواعد الوصول للبيانات

- مصدر البيانات الحالي: `lib/craftsmen.ts` (بيانات ثابتة).
- أي وصول للبيانات من الكومبوننتس يكون عبر الـ hooks في `hooks/useCraftsmen.ts`
  حتى يمكن استبدال المصدر بـ Supabase لاحقاً دون تغيير الكومبوننتس.
- التوثيق الكامل لنموذج البيانات موجود في `docs/data-model.md`.

## قواعد واجهة المستخدم

- الموقع Mobile-first بالكامل، واتجاه الصفحة RTL، واللغة عربية.
- النصوص بحجم 16px على الأقل (`text-base`)، وأزرار الاتصال min-height 48px.
- لا تُستخدم مكتبات أيقونات جاهزة للتخصصات — الأيقونات مرسومة يدوياً كـ SVG
  في `components/shared/CategoryIcon.tsx`.
- أيقونات الواجهة العامة (هاتف، موقع، صح، سهم، شمس/قمر، كاميرا...) من
  `lucide-react` عبر `components/shared/icons.tsx`.
- أيقونات البراند (واتساب/فيسبوك/انستغرام/تيك توك) SVG يدوي لأن Lucide
  لا يوفر أيقونات براند.
- الألوان من CSS variables في `app/globals.css` (background, foreground,
  muted, accent, action, card, border) ولا يُستخدم أي لون خارجها.

## التوثيق

- أي تغيير في نموذج البيانات يجب أن ينعكس في `docs/data-model.md`.
- حدّث `docs/plan.md` عند إنجاز أي مرحلة.
